[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router (SPA): rutas client-side → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
